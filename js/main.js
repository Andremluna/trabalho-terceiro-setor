/* =========================================================
   main.js — PONTO DE ENTRADA DO JAVASCRIPT
   Cada funcionalidade vive em seu próprio arquivo (js/modules/).
   Aqui só ligamos os módulos quando a página termina de carregar.
   ========================================================= */
document.addEventListener("DOMContentLoaded", function () {
  iniciarMenu();
  iniciarContadores();
  iniciarDoacao();
});
